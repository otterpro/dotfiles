echo off
@REM %chocolateyToolsLocation%\cygwin\bin\bash.exe -c -l "/home/%username%/bin/pushnotes.sh >> /home/%username%/log.txt 2>&1"
rem GIT=/usr/bin/git
rem #DATE=/usr/bin/date
rem DATE=`env date`
rem cd ~/notes
rem $GIT add -A
rem #$GIT commit -am"daily update `date`"
rem #$GIT commit -am"daily update from Macbook Pro $DATE"
rem $GIT commit -am"daily update from $OSTYPE $DATE"
rem $GIT pull --no-edit
rem $GIT push

